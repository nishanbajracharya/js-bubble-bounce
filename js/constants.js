var WIDTH = window.innerWidth || 1024;
var HEIGHT = window.innerHeight || 768;
var BALL_COUNT = 1000;

var MAX_SPEED = 1;
var MIN_SPEED = 8;

var MAX_ANGLE = 5;
var MIN_ANGLE = 85;

var MAX_RADIUS = 4;
var MIN_RADIUS = 10;